
function array2string( arr ) {

    var str = "[";

    for( var i = 0; i < arr.length; i++ ) {
	
	if( i == 0 )
	    str += " ";
	else
	    str += ", ";

	str += arr[i];

    }

    str += "]";

    return str;
}